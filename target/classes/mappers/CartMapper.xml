<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.com.kv.mappers.CartMapper">

	<insert id="register">

	INSERT INTO kvmall.cart
	(cmid, smid, pcode, pname, cnt, priceamt, regdate)
	VALUES(#{cmid}, #{cmid},#{pcode}, #{pname}, #{cnt}, #{priceamt},#{regdate});
	</insert>
	

	<insert id="saleregister">
		INSERT INTO kvmall.salelist
		(pcode, smid,cmid, pname, pric, cnt)
		VALUES(#{pcode}, #{smid},#{cmid}, #{pname}, #{pric},#{cnt});
		
		

	</insert>


</mapper>