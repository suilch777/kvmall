<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.km.dao">
	
	
	<insert id="insertkvproduct" >
				
		INSERT INTO kvproduct
		(pcode, pname, pcontent, price, dcprice, color, size, pic)
		VALUES(#{pcode},#{pname}, #{pcontent},#{price}, #{dcprice}, #{color}, #{size}, #{pic});
		
	</insert>
	
	
	
	
	
	
	
</mapper>



